# Agent vs user terminals

Agent Shell tool is separate from user PowerShell PID. User does interactive login.
Document channel in each turn. Optional `.cursor/*-terminal-session.json` metadata (no secrets).
